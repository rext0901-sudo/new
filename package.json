{
  "name": "aquasmart",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@vercel/postgres": "^0.10.0",
    "xml2js": "^0.6.2"
  },
  "scripts": {
    "build": "cd client && npm install && npm run build"
  }
}
